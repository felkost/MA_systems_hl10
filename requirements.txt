# Application dependencies, exact pins. Recorded at stage 1 from what
# `pip install` actually resolved on 2026-08-14 (Python 3.12.10), filtered to
# the application requirement names -- not the whole environment, which also
# holds transitive packages this file does not name directly.

# Protocols. a2a-sdk needs the [http-server] extra to serve at all: without
# it the route factories raise at import. acp-sdk was removed when the brief
# moved to A2A -- ACP itself was donated into A2A under the Linux Foundation
# in August 2025 and its repository is archived.
fastmcp==3.4.7
a2a-sdk[http-server]==1.1.2
langchain-mcp-adapters==0.3.2

# Agents
langchain==1.3.15
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-openai==1.5.1
pydantic==2.13.4
pydantic-settings==2.15.0

# Observability
langfuse==4.14.4
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-instrumentation-asgi==0.65b0

# Retrieval stack (ported from hl8)
chromadb==1.5.9
langchain-chroma==1.1.0
langchain-community==0.4.2
langchain-huggingface==1.2.2
rank-bm25==0.2.2
sentence-transformers==5.7.0
pypdf==6.16.1

# Tools
httpx==0.28.1
trafilatura==2.2.0
ddgs==9.14.4

# Evaluation (evals/golden/*.yaml, spec Sec13) -- present transitively via
# chromadb before this pin; named explicitly so the format the spec mandates
# does not depend on somebody else's dependency graph.
pyyaml==6.0.3

# Evaluation statistics (evals/statistics.py, stage 10b, decision D7) --
# both already present transitively via sentence-transformers and were
# unpinned, the same shape as the pyyaml finding above; the numbers a report
# cites must not depend on somebody else's dependency graph.
scipy==1.18.0
scikit-learn==1.9.0
